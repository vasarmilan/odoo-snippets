# -*- mode: snippet -*-
# name: oo_substitute_group
# key: oo_subs_group
# first kill the region, and then this will yank it back
# could not make it to work on my machine
# with `yas-selected-text`
# --
<group ${1:string="$2"}> 
 `(car kill-ring)`$0
</group>